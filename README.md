# Nikkah Invitation — Astro + GSAP

This is the rebuilt version using Astro + GSAP + custom CSS. Astro generates a static site; GSAP handles the cinematic opening and scroll choreography.

## Commands
npm install
npm run dev
npm run build

Deploy the generated `dist/` directory through Vercel. Replace the Google Maps and closing placeholders before launch.
